# Salin file ini jadi ".env" lalu isi nilainya. JANGAN commit file ".env" ke git.

# Port lokal untuk backend proxy ini
PORT=8787

# Origin front-end yang diizinkan memanggil backend ini (CORS).
# Isi dengan domain tempat phoveus-agent-os.html kamu di-hosting.
# Contoh: http://localhost:5500 atau https://username.github.io
ALLOWED_ORIGIN=http://localhost:5500

# --- Claude (opsional, untuk reasoning agent) ---
# Ambil dari https://console.anthropic.com/settings/keys
# Kalau dikosongkan, backend otomatis pakai fallback heuristik lokal.
ANTHROPIC_API_KEY=

# --- Binance Agent OS MCP (opsional, untuk eksekusi order sungguhan) ---
# URL endpoint MCP Binance Agent OS untuk akun/agent kamu.
BINANCE_AGENT_OS_URL=https://agent.binance.com/mcp/agentic

# Token akses yang didapat setelah menghubungkan agent ke Binance Agent OS
# (buat & kelola scope-nya dari dashboard Binance Agent OS kamu sendiri).
BINANCE_AGENT_OS_TOKEN=

# Kunci rahasia sederhana untuk melindungi endpoint admin/debug (mis. /api/tools)
ADMIN_DEBUG_KEY=ganti-dengan-string-acak-yang-panjang
