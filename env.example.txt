# Copy this file to ".env" and fill in the values. NEVER commit the real ".env" file.

# Local port for this backend proxy
PORT=8787

# Frontend origin allowed to call this backend (CORS).
# Set this to the domain where you host phoveus-agent-os.html.
# Examples: http://localhost:5500  or  https://username.github.io
ALLOWED_ORIGIN=http://localhost:5500

# --- Claude (optional, for agent reasoning) ---
# Get a key from https://console.anthropic.com/settings/keys
# Leave empty to use the built-in local heuristic fallback.
ANTHROPIC_API_KEY=

# --- Binance Agent OS MCP (optional, for real order execution) ---
# MCP endpoint for your agent / account
BINANCE_AGENT_OS_URL=https://agent.binance.com/mcp/agentic

# Access token obtained after connecting an agent in the Binance Agent OS dashboard.
# Manage scopes there (keep them as minimal as possible).
BINANCE_AGENT_OS_TOKEN=

# Simple secret used to protect the admin/debug endpoint (/api/tools)
ADMIN_DEBUG_KEY=replace-with-a-long-random-string
